<div class="row mt-3">
  <div class="col">
    <a href="/"><button class="btn btn-outline-info">Voltar</button></a>
  </div>
</div>

<div class="card mt-3">
  <div class="card-body">
    <h2>{{post.titulo}}</h2>
    <small>Publicado em: {{post.data}}</small>
    <h5 class="my-3">{{post.descricao}}</h5>
    <hr class="mt-3">
    <div class="card-text">
      {{post.conteudo}}
    </div>
  </div>
</div>
